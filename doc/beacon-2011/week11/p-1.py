# reminder: the % operator is known as the 'remainder' operator, with
# (N % M) equal to the remainder of N divided by M.  For example, 7 % 3 is
# 1.

for i in range(10):
    if i % 2 == 0:
        print 'fiz'
